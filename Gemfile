source "https://rubygems.org"

gem "jekyll", "~> 4.3"
gem "webrick"                 # Ruby 3 dropped webrick from stdlib; `jekyll serve` needs it

group :jekyll_plugins do
  gem "jekyll-remote-theme"   # fetches the Minimal Mistakes theme
  gem "jekyll-include-cache"  # required by Minimal Mistakes
  gem "jekyll-feed"
  gem "jekyll-sitemap"
end
